Program strings;
VAR
   name, msg : STRING;
   age : INTEGER;
   BEGIN
   Writeln('what is your name?');
   read(name);
   Writeln('what is your age');
   read(age);
   msg := 'hi' + name + 'It is cool that your are';
   writeln(msg, age,' years old');
   write('your message is ', length(msg),' long');
   for i ;

read(age);
   END.